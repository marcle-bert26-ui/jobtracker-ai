# JobTracker AI — backend
# Généré à partir des imports réels du code (voir README-backend.md)

fastapi>=0.110,<1.0
uvicorn[standard]>=0.29,<1.0
sqlalchemy>=2.0,<3.0
pydantic>=2.0,<3.0
python-dotenv>=1.0,<2.0
requests>=2.31,<3.0

# Lecture des emails Outlook Desktop via COM (Windows uniquement, pas
# besoin sous Linux/Mac si tu n'utilises que Yahoo/Gmail en IMAP).
pywin32>=306; sys_platform == "win32"

# Non utilisé par défaut (ancien flux OAuth Microsoft Graph, conservé au
# cas où tu voudrais y revenir un jour) :
# msal>=1.28,<2.0
